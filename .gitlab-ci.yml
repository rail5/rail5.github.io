image: alpine:latest

pages:
  stage: deploy
  script:
  - echo 'Nothing to do...'
  - cd public/deb/
  - echo "<html><body><div align=\"center\"><h1>Archive of BookThief+Liesel versions</h1><br><h3>Directory Listing:</h3><br><br></div>" > ./index.html
  - find -exec echo "<a href='{}'>{}</a><br>" \; >> ./index.html
  - echo "</body></html>" >> ./index.html
  artifacts:
    paths:
    - public
  only:
  - master
